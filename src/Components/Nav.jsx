const Nav = () => {
  return (
    <nav
      style={{
        display: "flex",
        justifyContent: "center",
        gap: "1rem",
        // flexDirection: "column",
      }}
    >
      <div>0. ABOUT</div>

      <div>1. SKILLS </div>

      <div>2. WORK</div>

      <div>3. PROJECTS </div>

      <div>4. CONTACT</div>

      <div>5. RESUME</div>
    </nav>
  );
};

export default Nav;
